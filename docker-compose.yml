version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
    volumes:
      - .:/app
      - /app/node_modules
      - /app/prisma/dev.db:/app/prisma/dev.db
    command: yarn dev

volumes:
  data: 